---
description: Разбор полного чужого лога X-Ray. Аргумент — путь к логу. Не чинить.
---

Разбор полного чужого лога. Аргумент после `/logfull` — путь к логу. Нет пути — спроси и остановись.

Дальше — тот же порядок, что у [`/crash`](crash.md): `docs/issues.md`, критерий `--archive`, карточка через `xraylog.py`, секция «Мои моды», этапы 1–3 по [`docs/plans/crash.md`](../../docs/plans/crash.md). Ничего не исправляй.

**Сверка с ожидающими подтверждения.** После карточки и `--mine` открой [`docs/awaiting-confirmation.md`](../../docs/awaiting-confirmation.md). Пройди список: встретилась ли какая-то из ожидаемых сигнатур (назови id/гард и мод). Встретилась — этот лог годится, чтобы проверить соответствующий гард на этом отчёте (есть/нет FATAL после фикса, есть ли `skip`/`wrapped` мода). Не встретилась — так и скажи: материал для этих гардов этот лог не даёт.
