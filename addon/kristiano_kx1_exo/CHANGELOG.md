# Kristiano KX-1 Exo Craft

## [1.5.5-c] — 2026-09-19

**Рабочие материалы вне addon/**

- `ICON_TZ.txt` и `lore/` перенесены в `docs/mods/kristiano_kx1_exo/` (STRUCT-004/005: в пакет не попадали). Правило: `docs/mods/README.md`.

## [1.5.5-b] — 2026-09-18

**Схрон с флешкой**

- Шанс постановки тайника с зашифрованной флешкой: **1%** (одноразовый ролл при первом подходящем ящике).
- Промах сжигает попытку (без ретраев каждые 15 с). Путь legend-флешки не тронут.

## [1.5.4-b] — 2026-09-18

**Вылет stack overflow на LoadRecipesLTX**

- Wrap `LoadRecipesLTX`: `ensure_load_wrap` держит KX-1 outermost; на `actor_on_first_update` возвращает top при потере; не захватывает собственный wrap.
- Совместно с `fix_milspec_exo_craft` 1.0.4: убран цикл milspec↔kristiano при просмотре ТБ/гайдов.
- Не затронуто: рецепты, PSU, схрон, servo, Toxic Air.

## [1.5.3-c] — 2026-09-16

**Крафт + баллоны Toxic Air**

- Крафт шага 2: убран `af_aac` (прикладной контейнер); нужно **7× `af_aam`** (прикладной модуль).
- Toxic Air баллоны: mode-flip `airgear_items` (filter → tank+`hud_sci` на пояс, обратно при снятии) + wrap `get_active_tank_gear`. Фильтры без баллона на поясе по-прежнему работают.

## [1.5.3-b] — 2026-09-16

**Toxic Air, каркас, крафт-модули**

- Toxic Air: костюм на `hud_kx1` (все тиры фильтров T1–T3); баллоны sci через `zzz_kristiano_kx1_toxic_air.script` (wrap `get_active_tank_gear`). Фильтр и баллон — две отдельные возможности (одновременно Toxic Air не держит).
- Каркас `kristiano_kx1_frame`: `II_ATTCH` / `i_device` вместо `tch_letter` (больше не «непрочитанный» документ).
- Описание каркаса в стиле KX-1 с пунктами на `•`.
- Крафт шага 2: вместо 7× `af_iam` (самодельные) — 7× `af_aac` (ошибочно контейнер; исправлено в 1.5.3-c на `af_aam`).
- Не затронуто: флешка/чертёж, схрон, апгрейды, immunities.

## [1.5.2-b] — 2026-09-15

**Прочность + нижние апгрейды у техника**

- `immunities_sect`: вместо хрупкого Exo System (strike 0.2 / wound 0.3) — лучше мерк-Носорога (strike 0.001 / wound 0.006 / explosion 0.025).
- 5-я колонка после carry: своя цепочка `up_gr_fifthcd_kx1` / `up_gr_fifthef_kx1` (psi 20 / rad 10 / +1 арт-слот), не vanilla `*_exo_1` с helm-строками и микро-дельтой.
- Описание: блок «Особенности» переписан понятнее для игроков; дефисы заменены на точки; добавлен пункт про вывод радиации на полном заряде. Цвета те же.
- SMS при первом надевании: своя иконка `ui_icon_news_kristiano_kx1` (ключ `kristiano_kx1`); приветствие `kristiano_welcome` не трогали.
- Не затронуто: крафт, схрон, БП, колонки 1–4 exolight.
- После обновления нижние 3 узла лучше переустановить у техника (старые `up_fifth*_exo_1` на сейве остаются в данных предмета, новые — отдельные секции).

## [1.5.1-b] — 2026-09-15

**Ревью перед тестом**

- Схрон: флаг успеха только если `get_random_stash` нашёл ящик (иначе ретрай, без ложного SMS).
- Tips через guard `news_manager` (decrypt / legend / stash / disassemble).
- `actor_on_update` троттл 1 с (не каждый кадр).
- `is_kx1` без лишней проверки `obj.section`.

## [1.5.0-b] — 2026-09-15

**Остаток блока B**

- B3: одноразовый схрон с зашифрованной флешкой (`treasure_manager.create_random_stash`), флаг в сейве.
- B4: `can_trade = false` на костюм / флешку / чертёж / каркас (в trade-листах и так не было).
- B7: `con_parts_list` с `prt_o_retardant_3`; при разборке гарантированный бонус (БП + текстолит + конденсаторы).
- B8: двухшаговый крафт — `kristiano_kx1_frame` → готовый KX-1.
- БП: на полном заряде лёгкий scrub радиации (`change_radiation`).
- B10 баллоны: **не сделано** (нет verified API на костюмах).

## [1.4.3-b] — 2026-09-15

**B1 иконка**

- Своя `ui\ui_icon_kristiano_kx1` (атлас 128×256 DXT5, слот 2×3).
- Взят UI S.T.A.L.K.E.R. 2 **нейтрального** экзо (`Exoskeleton_Neutral`), не Monolith Diamond — под одиночек/нейтрал.
- В секции костюма прописаны `icons_texture` / `inv_grid_*`.

## [1.4.2] — 2026-09-15

**Исправлено**

- `gamedata/configs/text/rus/st_items_kristiano_kx1_exo.xml`: убраны UTF-8 replacement (`U+FFFD`), из‑за которых линтер валил CI (ENC-004). Русские строки восстановлены из последнего целого коммита (`c1adeba`), encyclopedia/SMS — из `lore/kx1_encyclopedia.txt` и ENG; в описание добавлены строки про расход БП и тихие серво (как в ENG).

## [1.4.1-b] — 2026-09-14

**Вау-пакет: звук + БП**

- B5: фирменный серво-пресет только на KX-1 (тише / ниже тон шага). У Exo System нет пресета на секцию — только light/middle/heavy по весу; сделан лёгкий wrap `exo_servo_sounds`, не форк системы.
- B6: `suit_drain` **0.018** вместо 0.025 (~28% экономичнее Raytheon). Уникальный бафф на полном заряде не трогали.

## [1.4.0-b] — 2026-09-14

**Блок B (мини-пакет, без своей DDS-иконки)**

- B1: ТЗ иконки `ICON_TZ.txt` (ячейка 50px, атлас 2×3, поля `icons_texture`/`inv_grid_*`; источник — Anomaly Modding Book).
- B2: статья PDA `encyclopedia_items_kristiano_kx1` + лор-файл `lore/kx1_encyclopedia.txt` (править лор там).
- B9: одноразовое SMS от Кристиано при первом получении/надевании KX-1 (`dynamic_news_helper` / fallback `news_manager`), флаг в сейве.

Не сделано: B3/B4/B7/B8/B10.

## [1.3.0-test] — 2026-09-14

**Тестовый пакет этапа 2 (блок A)**

- A1: базовые арт-слоты **7** (апгрейд +arty в дереве нет).
- A2: в крафт добавлено **7× `af_iam`** (синхронно с `artefact_count`).
- A3 TEST: `mag_limit` на секции **3 / 7 / 7** (было как у экзо 2/5/5); 5-й апгрейд — **переносимый вес +10**, не бег (спринт уже свободен). Апгрейд магазинов через upgrade-секцию Mags не читает — без monkey-patch.
- A4: флаг `helmet_avaliable = false` без изменений; описание уточнено (UI «головной убор: доступно» = встроенный есть).

Блок B не трогали.

## [1.2.0] — 2026-09-14

**Изменено**

- Кости: своя секция `actor_armor_kristiano_kx1` (Носорог+ ~12%), `fire_wound_protection = 1.05`.
- Встроенный «Спартанец»: `helmet_avaliable = false` + часть `helm_spartan` в `con_parts_list` / разборке (как у Носорога).
- Электро: `shock_protection = 2.2` → 55% в UI (`electra_zone_max_power = 4.0`).
- Хим 25% (`0.0625`), разрыв 45% (`0.99`). Слоты/вес/+бег без изменений.
- Апгрейды как у XOS 3: 1-й слот разрыв, 5-й бег. Старые `up_*_kx1_*` оставлены для сейвов.

**Цена:** `500000` не трогали — после баффа защиты можно пересмотреть отдельно.

## [1.1.1] — 2026-09-14

**Исправлено**

- Чертёж/флешка открывают крафт: энциклопедия + interactivity (как milspec PDA). Раньше UI «прочитано», CSI крафт оставался закрыт.
- Расшифровка сразу делает `unlock_note`. Чертёж уже в инвентаре на старом сейве — разлок при загрузке.
- Описание без `\t`: Гидра — один учёный; Кристиано — великий научный сотрудник Зоны; секретная разработка.

## [1.1.0] — 2026-09-14

**Добавлено / изменено**

- Статы KX-1: кости Носорога, электро ~55%, хим, 4 слота, бег, реген, нейтрал.
- Апгрейды: пси +20% / слот арта.
- Флешка, дроп, Легенда, 75к расшифровка.
- Рецепт с артами + дефолтный recharge-БП.

**Не сделано**

- Баллоны научного экзо.

## [1.0.0] — 2026-09-14

Базовый KX-1 + крафт лоскута.
